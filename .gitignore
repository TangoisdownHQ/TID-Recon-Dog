# deps & build
node_modules/
dist/

# runtime data / secrets / artifacts
runtime/
runtime-fleet/
logs/
uploads/
exports/
ct/
*.log
.env
host.key*

# ML artifacts (multi-GB models, vendored llama.cpp, nested-repo pipeline) —
# keep only the docs; the trainer pipeline is its own repo.
mlops/*
!mlops/RETRAIN.md
!mlops/INFERENCE.md
# Versioned reference copies of the ML pipeline scripts. The live pipeline
# (mlops/tidrc-ml-pipeline/) is a separate local git repo with no remote, so
# these copies are the actual backup + review surface. Models/venv/build stay out.
!mlops/pipeline-scripts/

# terraform state
infra/**/.terraform/
infra/**/*.tfstate*
infra/**/*.tfvars

# editor / os
.DS_Store
.codex/

# agent/editor state
.codex
.claude
.claude/
CLAUDE.md
